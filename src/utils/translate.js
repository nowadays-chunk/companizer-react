// Utility for translating text
export const translate = (text, language = 'en') => {
    // Simple pass-through translation utility
    // In a real app, this would use i18n libraries or translation dictionaries
    return text;
};

export default translate;
